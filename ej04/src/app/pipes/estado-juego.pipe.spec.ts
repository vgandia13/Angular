import { EstadoJuegoPipe } from './estado-juego.pipe';

describe('EstadoJuegoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadoJuegoPipe();
    expect(pipe).toBeTruthy();
  });
});
